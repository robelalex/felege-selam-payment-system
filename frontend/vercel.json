{
    "installCommand": "npm install --legacy-peer-deps",
    "buildCommand": "DISABLE_ESLINT_PLUGIN=true",
    "outputDirectory": "build",
    "rewrites": [
        {
            "source": "/api/(.*)",
            "destination": "https://felege-selam-payment-system.onrender.com/api/$1"
        }
    ]
}